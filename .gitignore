node_modules/
dist/
build/
.env
*.log
.DS_Store
coverage/
.angular/
tmp/
.idea/
.vscode/
*.tgz
test-results/

# Deploy SSH key (project-local, kept out of git)
id_ed25519
id_ed25519.pub

# Local deploy credentials (never commit)
deploy-secrets.local

# Agent scratchpad (SSH helper, build tarballs, ad-hoc SQL) — never commit
scratchpad/

# local deploy venv (paramiko helper)
deploy/.venv/

# built mobile APKs (binary, rebuild with ./deploy/build-apk.sh)
dist-apk/
*.apk

# Design canvas bundle — generated, ~2MB of editor payload
docs/ecommerce/design/sabihas-ethnic-storefront-directions.html
