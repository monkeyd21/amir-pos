# Deploy config template. Copy to `deploy-secrets.local` and fill in the password.
# deploy-secrets.local is gitignored (never commit real secrets).
# Production deploy credentials — LOCAL ONLY, gitignored, never commit.
# Box: Contabo VPS — erp.sabihasethnic.com (see docs/deploy-notes.md)
DEPLOY_HOST=147.93.169.149
DEPLOY_SSH_USER=root
DEPLOY_SSH_PASSWORD=
